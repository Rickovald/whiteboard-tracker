{
  "name": "whiteboard-tracker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"cd host && npm run dev\" \"cd svs/drawer && npm run dev\"",
    "build": "cd host && npm run build",
    "start": "concurrently \"cd host && npm run preview\" \"cd svs/drawer && npm run start\"",
    "preview": "npm run build && npm run start"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}